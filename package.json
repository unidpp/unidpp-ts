{
  "name": "unidpp-ts",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "description": "UniDPP TypeScript SDK: model types, resolver linkset handling, registry/passport clients, in-browser Tier-A verification",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p ."
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^3.0.0"
  }
}
